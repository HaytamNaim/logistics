# FastAPI & server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
sqlalchemy>=2.0.25
asyncpg>=0.29.0
psycopg2-binary>=2.9.9
alembic>=1.13.0

# Auth & security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0,<4.1
python-multipart>=0.0.6

# Config & validation
pydantic[email]>=2.5.0
pydantic-settings>=2.1.0

# Redis (cache, pub/sub, driver availability)
redis>=5.0.0

# Background tasks (optional Celery alternative: in-process or Redis queue)
celery[redis]>=5.3.0

# HTTP client for Maps API (later)
httpx>=0.26.0

# UUID
uuid6>=2024.1.0
